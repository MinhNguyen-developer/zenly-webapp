-- Initialization SQL script for PostgreSQL
-- This runs when the database is first created

-- Create extensions if needed
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Database is already created by POSTGRES_DB env variable
-- This file can be used for additional initialization

-- Example: Create additional users or schemas if needed
-- CREATE USER app_user WITH PASSWORD 'app_password';
-- GRANT ALL PRIVILEGES ON DATABASE zenly TO app_user;

